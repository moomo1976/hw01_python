Domácí úkoly

Projekt č. 1: vyberte si jedno z možných zadání (a,b nebo c). 
Realizujte kompletní program. Pro každý projekt připravte jak
funkce tak příklady použití těchto funkcí.

vytvořte program kalkulačka
funkce
secti(a, b)
odecti(a, b)
vynasob(a, b)
vydel(a, b)
mocnina(x, n)
každá funkce bude vracet výsledek do proměnné

vytvořte program který bude šifrovat (Caesarova šifra)
vše bude založeno na dvou funkcích 
def sifruj (text)
def desifruj (zasifrovany_text)
obě funkce budou vracet výsledek v podobě řetězce do proměnné 
budete potřebovat funkce pro práci se znaky
print(chr(65))
print(ord('A'))
ch = ord('A')+3
print(chr(ch))
pro ověření funkčnosti můžete použít tento převod

vlastní projekt, který si chcete naprogramovat (bonus)
musí obsahovat funkce
